package com.copasso.cocobill.model.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.copasso.cocobill.model.bean.BSort;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BSORT".
*/
public class BSortDao extends AbstractDao<BSort, Integer> {

    public static final String TABLENAME = "BSORT";

    /**
     * Properties of entity BSort.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", true, "ID");
        public final static Property Uid = new Property(1, Integer.class, "uid", false, "UID");
        public final static Property SortName = new Property(2, String.class, "sortName", false, "SORT_NAME");
        public final static Property SortImg = new Property(3, String.class, "sortImg", false, "SORT_IMG");
        public final static Property Priority = new Property(4, Integer.class, "priority", false, "PRIORITY");
        public final static Property Income = new Property(5, Boolean.class, "income", false, "INCOME");
    }


    public BSortDao(DaoConfig config) {
        super(config);
    }
    
    public BSortDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BSORT\" (" + //
                "\"ID\" INTEGER PRIMARY KEY ," + // 0: id
                "\"UID\" INTEGER," + // 1: uid
                "\"SORT_NAME\" TEXT," + // 2: sortName
                "\"SORT_IMG\" TEXT," + // 3: sortImg
                "\"PRIORITY\" INTEGER," + // 4: priority
                "\"INCOME\" INTEGER);"); // 5: income
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BSORT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BSort entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer uid = entity.getUid();
        if (uid != null) {
            stmt.bindLong(2, uid);
        }
 
        String sortName = entity.getSortName();
        if (sortName != null) {
            stmt.bindString(3, sortName);
        }
 
        String sortImg = entity.getSortImg();
        if (sortImg != null) {
            stmt.bindString(4, sortImg);
        }
 
        Integer priority = entity.getPriority();
        if (priority != null) {
            stmt.bindLong(5, priority);
        }
 
        Boolean income = entity.getIncome();
        if (income != null) {
            stmt.bindLong(6, income ? 1L: 0L);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BSort entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer uid = entity.getUid();
        if (uid != null) {
            stmt.bindLong(2, uid);
        }
 
        String sortName = entity.getSortName();
        if (sortName != null) {
            stmt.bindString(3, sortName);
        }
 
        String sortImg = entity.getSortImg();
        if (sortImg != null) {
            stmt.bindString(4, sortImg);
        }
 
        Integer priority = entity.getPriority();
        if (priority != null) {
            stmt.bindLong(5, priority);
        }
 
        Boolean income = entity.getIncome();
        if (income != null) {
            stmt.bindLong(6, income ? 1L: 0L);
        }
    }

    @Override
    public Integer readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0);
    }    

    @Override
    public BSort readEntity(Cursor cursor, int offset) {
        BSort entity = new BSort( //
            cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // uid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // sortName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // sortImg
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // priority
            cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0 // income
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BSort entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setUid(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setSortName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSortImg(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setPriority(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setIncome(cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0);
     }
    
    @Override
    protected final Integer updateKeyAfterInsert(BSort entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public Integer getKey(BSort entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(BSort entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
