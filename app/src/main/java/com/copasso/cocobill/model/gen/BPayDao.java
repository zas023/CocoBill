package com.copasso.cocobill.model.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.copasso.cocobill.model.bean.BPay;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BPAY".
*/
public class BPayDao extends AbstractDao<BPay, Integer> {

    public static final String TABLENAME = "BPAY";

    /**
     * Properties of entity BPay.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", true, "ID");
        public final static Property Uid = new Property(1, Integer.class, "uid", false, "UID");
        public final static Property PayName = new Property(2, String.class, "payName", false, "PAY_NAME");
        public final static Property PayImg = new Property(3, String.class, "payImg", false, "PAY_IMG");
        public final static Property PayNum = new Property(4, String.class, "payNum", false, "PAY_NUM");
    }


    public BPayDao(DaoConfig config) {
        super(config);
    }
    
    public BPayDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BPAY\" (" + //
                "\"ID\" INTEGER PRIMARY KEY ," + // 0: id
                "\"UID\" INTEGER," + // 1: uid
                "\"PAY_NAME\" TEXT," + // 2: payName
                "\"PAY_IMG\" TEXT," + // 3: payImg
                "\"PAY_NUM\" TEXT);"); // 4: payNum
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BPAY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BPay entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer uid = entity.getUid();
        if (uid != null) {
            stmt.bindLong(2, uid);
        }
 
        String payName = entity.getPayName();
        if (payName != null) {
            stmt.bindString(3, payName);
        }
 
        String payImg = entity.getPayImg();
        if (payImg != null) {
            stmt.bindString(4, payImg);
        }
 
        String payNum = entity.getPayNum();
        if (payNum != null) {
            stmt.bindString(5, payNum);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BPay entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer uid = entity.getUid();
        if (uid != null) {
            stmt.bindLong(2, uid);
        }
 
        String payName = entity.getPayName();
        if (payName != null) {
            stmt.bindString(3, payName);
        }
 
        String payImg = entity.getPayImg();
        if (payImg != null) {
            stmt.bindString(4, payImg);
        }
 
        String payNum = entity.getPayNum();
        if (payNum != null) {
            stmt.bindString(5, payNum);
        }
    }

    @Override
    public Integer readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0);
    }    

    @Override
    public BPay readEntity(Cursor cursor, int offset) {
        BPay entity = new BPay( //
            cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // uid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // payName
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // payImg
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // payNum
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BPay entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setUid(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setPayName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setPayImg(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setPayNum(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Integer updateKeyAfterInsert(BPay entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public Integer getKey(BPay entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(BPay entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
